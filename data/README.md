# Data

Place the project datasets here:

- train_data.jsonl
- validation_data.jsonl
- test_data.json

Training and validation use chat-format records with system, user, and assistant messages. The test file contains held-out tickets and expected labels.

The original project data should be copied here rather than regenerated so the repository reproduces the same experiment.
